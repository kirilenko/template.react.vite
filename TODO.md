- [ ] Обработка несуществующих id в URL (ручной ввод, удалённая запись): если `GET /news/:id` вернул 404 или пустой ответ — редиректить на 404-страницу, а не показывать «News not found» внутри макета
